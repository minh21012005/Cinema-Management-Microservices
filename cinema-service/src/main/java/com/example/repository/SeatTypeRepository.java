package com.example.repository;

import com.example.domain.entity.SeatType;
import org.springframework.stereotype.Repository;

@Repository
public interface SeatTypeRepository extends BaseRepository<SeatType, Long> {
}
