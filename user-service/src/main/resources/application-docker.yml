server:
  port: 8082

spring:
  application:
    name: user-service
  data:
    redis:
      host: redis
      port: 6379

  datasource:
    url: jdbc:mysql://mysql-user:3306/user_db
    username: root
    password: 21012005
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

app:
  rabbitmq:
    exchange: user.exchange
    send-queue: user.admin.created.queue
    listen-queue: user.created.queue
    listen-queue-update: user.admin.updated.queue
    send-routing-key: user.admin.created
    listen-routing-key: user.created
    listen-routing-key-update: user.updated

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
  instance:
    prefer-ip-address: false
    hostname: user-service

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    prometheus:
      enabled: true

minhnb:
  jwt:
    base64-secret: Muw9kQMoEFTnfKm5sHbvqus3GvSYF4mSCL3ay11sjkRGLpMnvBTpsRF/bDHZmuI5ROhJ23QuZhOb1Cx3OZxy0w==
    access-token-validity-in-seconds: 1800
    refresh-token-validity-in-seconds: 2592000